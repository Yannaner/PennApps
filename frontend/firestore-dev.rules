rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Hackathon development rules - completely open for authenticated users
    // Any authenticated user can read and write any document anywhere
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}